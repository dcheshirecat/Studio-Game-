name: Build Android APKs

on:
  push:
    branches: [main, feature/*]
  workflow_dispatch:

env:
  UNITY_VERSION: 2022.3.20f1
  UNITY_IMAGE: unityci/editor:ubuntu-2022.3.20f1-android-3

jobs:
  # ── Build Heal (Therapeutic) variant ─────────────────────────────
  build-heal:
    name: Build Heal APK
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost /opt/hostedtoolcache
          sudo apt-get clean
          df -h /

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Cache Unity Library (Heal)
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-Android-Heal-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-Android-Heal-
            Library-Android-

      - name: Write credentials
        run: |
          echo '${{ secrets.UNITY_EMAIL }}' > /tmp/unity_email.txt
          echo '${{ secrets.UNITY_PASSWORD }}' > /tmp/unity_password.txt

      - name: Activate and Build Heal APK
        run: |
          docker pull $UNITY_IMAGE

          docker run --rm \
            -v "$PWD:/project" \
            -v "/tmp/unity_email.txt:/tmp/unity_email.txt:ro" \
            -v "/tmp/unity_password.txt:/tmp/unity_password.txt:ro" \
            -w /project \
            $UNITY_IMAGE \
            bash -c '
              UEMAIL=$(cat /tmp/unity_email.txt)
              UPASS=$(cat /tmp/unity_password.txt)

              echo "=== Activating Unity License ==="
              unity-editor -batchmode -nographics \
                -username "$UEMAIL" \
                -password "$UPASS" \
                -quit \
                -logFile /dev/stdout || true

              echo "=== Building Heal APK ==="
              unity-editor -batchmode -nographics \
                -projectPath /project \
                -executeMethod EndlessBeloved.Editor.BuildScript.BuildHeal \
                -buildTarget Android \
                -logFile /dev/stdout \
                -quit
            '

      - name: Cleanup credentials
        if: always()
        run: rm -f /tmp/unity_email.txt /tmp/unity_password.txt

      - name: Upload Heal APK
        uses: actions/upload-artifact@v4
        with:
          name: EndlessBeloved-Heal-APK
          path: build/Android/*.apk
          retention-days: 14

  # ── Build Dark (Fantasy) variant ────────────────────────────────
  build-dark:
    name: Build Dark APK
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /usr/local/share/boost /opt/hostedtoolcache
          sudo apt-get clean
          df -h /

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Cache Unity Library (Dark)
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-Android-Dark-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-Android-Dark-
            Library-Android-

      - name: Write credentials
        run: |
          echo '${{ secrets.UNITY_EMAIL }}' > /tmp/unity_email.txt
          echo '${{ secrets.UNITY_PASSWORD }}' > /tmp/unity_password.txt

      - name: Activate and Build Dark APK
        run: |
          docker pull $UNITY_IMAGE

          docker run --rm \
            -v "$PWD:/project" \
            -v "/tmp/unity_email.txt:/tmp/unity_email.txt:ro" \
            -v "/tmp/unity_password.txt:/tmp/unity_password.txt:ro" \
            -w /project \
            $UNITY_IMAGE \
            bash -c '
              UEMAIL=$(cat /tmp/unity_email.txt)
              UPASS=$(cat /tmp/unity_password.txt)

              echo "=== Activating Unity License ==="
              unity-editor -batchmode -nographics \
                -username "$UEMAIL" \
                -password "$UPASS" \
                -quit \
                -logFile /dev/stdout || true

              echo "=== Building Dark APK ==="
              unity-editor -batchmode -nographics \
                -projectPath /project \
                -executeMethod EndlessBeloved.Editor.BuildScript.BuildDark \
                -buildTarget Android \
                -logFile /dev/stdout \
                -quit
            '

      - name: Cleanup credentials
        if: always()
        run: rm -f /tmp/unity_email.txt /tmp/unity_password.txt

      - name: Upload Dark APK
        uses: actions/upload-artifact@v4
        with:
          name: EndlessBeloved-Dark-APK
          path: build/Android/*.apk
          retention-days: 14
